#
eval_every: 10_000

# train init process
batch_size: 64
n_pretraining_iters: 5_000
n_training_iters: 500_000

# model params
sigma: 0.01
control_weight: .5
acc_weight: 0.

# net
n_layers: 5
nfilter_max: 512 

optimizer:
  learning_rate: 1e-4
  b1: 0.5
  b2: 0.5