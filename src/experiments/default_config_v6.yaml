#
eval_every: 1000

# train init process
batch_size: 48
n_training_iters: 500_000
collect_buffer_iters: 1000
update_potential_every: 2

# model params
reg_weight: 0.
control_weight: 1.0

# net
n_layers: 5

optimizer:
  learning_rate: 5e-5
  b1: 0.5
  b2: 0.5
